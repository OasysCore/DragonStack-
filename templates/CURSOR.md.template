# CURSOR.md Template / CURSOR.md 模板

> DragonStack configuration for Cursor / Cursor 的龙栈配置

---

## Platform / 平台

- **Name**: Cursor
- **Vendor**: Anysphere
- **Models**: GPT-4, Claude 3.5 Sonnet
- **Website**: https://cursor.sh/

---

## DragonStack Skills / 龙栈技能

Available commands / 可用命令:

```
/brainstorm        - Product ideation / 产品构思
/code-review       - Code review / 代码审查
/ship              - Ship code / 发布代码

/careful           - Safety mode / 安全模式
/freeze            - Scope lock / 范围锁定
/guard             - Full safety / 完整安全
```

---

## Usage / 使用

1. Open Cursor / 打开 Cursor
2. Press `Cmd/Ctrl + L` for AI chat / 按 `Cmd/Ctrl + L` 打开 AI 对话
3. Type any DragonStack command / 输入任意龙栈命令
4. Use Composer for multi-file edits / 使用 Composer 进行多文件编辑

---

## Tips / 技巧

- Use **Composer** (`Cmd/Ctrl + I`) for large changes / 使用 **Composer** 进行大改动
- Enable **Agent Mode** for autonomous execution / 启用 **Agent 模式** 进行自主执行
- Use `@` to reference files / 使用 `@` 引用文件

---

## Configuration / 配置

```yaml
# .dragonstack/config.yaml
platform: cursor
model: gpt-4
features:
  composer_mode: true
  agent_mode: true
```

---

*Generated by DragonStack / 由龙栈生成*
